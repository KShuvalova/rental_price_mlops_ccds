services:
  api:
    build: .
    container_name: rental-price-api
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
      - ./reports:/app/reports
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped